{
  "name": "simkl-new-episodes-addon",
  "version": "1.8.1",
  "private": true,
  "type": "module",
  "description": "A personal Stremio/Nuvio catalog that bumps tracked anime on every new episode, including revived Completed titles.",
  "scripts": {
    "authorize": "node scripts/authorize.mjs",
    "build:placeholder": "node scripts/build-placeholder.mjs",
    "keepalive": "node scripts/update-keepalive.mjs",
    "refresh": "node scripts/update.mjs",
    "test": "node --test",
    "verify": "node scripts/verify-output.mjs"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "license": "MIT",
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
